version: '3.8'

services:
  app:
    image: ${APP_NAME}:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${APP_NAME}
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
    restart: unless-stopped